#LOCAL_LDLIBS := -llog

FILE(GLOB SRC *.cpp)
FILE(GLOB HEADERS *.h)

ADD_DEFINITIONS(-DENABLE_LOGS)

INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR}/unrar)

ADD_LIBRARY(unrar-jni SHARED ${SRC} ${HEADERS})

TARGET_LINK_LIBRARIES(unrar-jni unrar log)
